# Copy to .env.coolify and fill in. Do not commit .env.coolify.
# Used by: scripts/deploy-coolify.sh

# Coolify instance URL (no trailing slash)
COOLIFY_URL=http://192.168.1.66:8000

# API token from Coolify: Keys & Tokens -> API tokens
COOLIFY_TOKEN=

# Public Git repository URL (must be public for create-public-application API)
GIT_REPOSITORY=https://github.com/your-org/wormhole
GIT_BRANCH=main

# Optional: override auto-detection (script uses first server and first project if unset)
# SERVER_UUID=
# PROJECT_UUID=
# ENVIRONMENT_NAME=production
